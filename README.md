# Turath Website

Mobile-first Next.js and Tailwind skeleton for the Turath oil seeds brand.

## Setup

```bash
npm install
npm run dev
```

## WhatsApp number

Create `.env.local` and set:

```bash
NEXT_PUBLIC_WHATSAPP_NUMBER=212708987943
```

Use the international format without `+`.
